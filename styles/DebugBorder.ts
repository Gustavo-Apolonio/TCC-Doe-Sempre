import { StyleSheet } from "react-native";

const Style = StyleSheet.create({
  DebugBorder: {
    borderStyle: 'solid',
    borderWidth: 1,
    borderColor: 'black'
  }
});

export default Style.DebugBorder;
